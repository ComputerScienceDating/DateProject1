<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
<!--James: Style section with html shortcuts and style changes-->
<style>
    .row:after {
        content: "";
        display: table;
        clear: both;
    }
    .column {
        padding: 10px;
    }
    .left {
        width: 45%;
        float: left;
        border-right: 3px solid black;
    }
    .right {
        width: 45%;
        float: right;
    }
    .headerArea {
        width: 100%;
        height: 110px;
    }
    .logoL {
        width: 10%;
        height: 105px;
        top: 0px;
        float: left;
        overflow: auto;
    }
    .bannerR {
        width: 85%;
        height: 105x;
        top: 0px;
        float: right;
        overflow: auto;
    }
</style>

</head>
<body>
    <div class="headerArea">
        <div class="logoL">
            <a href="../Home/Index">
                <img src="~/Content/Images/LogoTemp.png" style="width:100%; height:100px"/>
            </a>
        </div>
        <div class="bannerR">
            <img src="~/Content/Images/BannerTest.png" style="width:100%; height:100px"/>
        </div>
    </div>
    <div class="navbar navbar-inverse">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("CLASS-MATES", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li>@Html.ActionLink("Profile", "Index", "People")</li>
                        <li>@Html.ActionLink("Matches", "Index", "Commons")</li>
                        <li>@Html.ActionLink("Messages", "Index", "Messages")</li>
                        <li>@Html.ActionLink("Contact Us", "Contact", "Home")</li>
                    }
                    else
                    {
                        <li>@Html.ActionLink("About", "About", "Home")</li>
                        <li>@Html.ActionLink("Contact", "Contact", "Home")</li>
                    }
                </ul>
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - My ASP.NET Application</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
